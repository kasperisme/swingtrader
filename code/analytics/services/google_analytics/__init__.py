"""google_analytics — read-only GA4 Data API + Search Console insight for the site.

Service-account auth (see client.py). Mirrors the meta_ads / posthog_analytics
service layout: `client` (auth/config), `ga4`, `search_console`, `discover`, `cli`.
"""
