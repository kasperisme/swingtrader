"""performance — the unified cross-platform performance foundation.

Joins GA4, Search Console, Meta Ads, Supabase leads and PostHog along the funnel
into one snapshot + routed action flags. Mirrors the other service modules:
`sources` (adapters), `snapshot` (aggregate + flags + render), `cli`.
"""
