Ops:

[x] Make sure openclaw does use whats app to communicate with subagents
[ ] Send daily operational reports

Dev:
Prio 1:
[x] Make the network graph fill out the space better
[x] Implement stripe
[x] Setup X ingestion (show trump x posts in the graph)
[x] Ensure that backfilling backfills on stream and not total
[x] Ensure that dry days on streams are tracked
[x] Move articles to own subpage and have protected root be ops center for users
[x] Make the pricing button on the landing page not redirect to stripe, but to signup page
[x] Add news trend personas to chart AI that gets company vector and news trend data from supabase
[x] Ai chat should fill entire height, and be collapsible
[x] The narrative generator fails as too much data is being embedded into the prompt
[x] (Agent) Timezones are not respected in the job
[x] (AI Chat) When starting a new prompt, navigating to another chart shouldnt reset the prompt, but it should still execute the prompt
[ ] (UI) Implement pricing gates throughout the application and API
[ ] (Caching) Caching isnt isnt updating correctly (news trends didnt show the last 24 hours)
[ ] (AI Chat) Implement small prompt support in AI chat. Currently all personas are engaged in every prompt
[ ] (AI Chat) Add granularity to chart, and make sure the AI applies the granularity to the prompt
[ ] (AI Chat) Screening agent is slow (find way to make the agent repond faster e.g. limit personas in UI and let it be a background agent, show in UI like in claude code subagent flow)
[ ] (AI Chat) Screening several stucks is slow (implement a batch process for the agent to run)
[ ] (Agent) In the agent, allow the user to select tickers and screenings for the agent to monitor
[ ] (Agent) Have agent activation windows (e.g. it should scan every 15 minutes when intramarket, but otherwise not)
[ ] (Design) Optimize for mobile
[ ] (Agent) Setup market wide screenings (start with predefined screenings and let the agent run them, and in the future let the user create their own screenings using the screening)

Prio 2:
[x] Get embedding searching to work with the ollama model
[x] Make sure IBD screener script runs through API
[x] Implement anlytical agent in screening sites as well (copy from chart)
[ ] In the daily narrative graph, add an AI feature to summerize the narrative in the selected period
[ ] Refactor analytics scripts to reduce redundancy
[ ] Set up company vector updating
[ ] Seperate newsimpactscreener FMP integration with personal API key
[ ] Ensure robustness of telegram bots
[ ] Make sure network graph doesnt update when user selects something in current scope

Marketing:
[x] Set up pricing and early sign up offer
[ ] Setup instagram and tiktok bots to post about site
[ ] Setup competition flow with real price money

Inspiration:

How to use Claude To Gain a Huge Day Trading Edge (https://www.youtube.com/watch?v=Rqmdw4xyIMM):
[ ] Custom alarms for charts e.g. agents that users can deploy to their charts
